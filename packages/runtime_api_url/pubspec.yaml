name: runtime_api_url
description: Shared helpers for normalizing runtime API base URLs across apps.
publish_to: 'none'
version: 0.2.0

environment:
  sdk: ">=3.11.0 <4.0.0"

dev_dependencies:
  test: ^1.25.8
