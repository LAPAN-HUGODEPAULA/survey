# MongoDB Credentials (for Local)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=secret

# MongoDB Connection URIs
# Local: Uses the credentials above and the service name 'mongodb'
MONGO_URI_LOCAL=mongodb://admin:secret@mongodb:27017
# Atlas: Replace with your actual Atlas connection string
MONGO_URI_ATLAS=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Active Configuration (Set automatically by deploy_docker.sh)
# MONGO_URI=
# COMPOSE_PROFILES=

# Other Settings
FLAVOR=dockerVps
